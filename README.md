# 天体角距离计算器

一个学习C语言软件开发流程的示例项目，计算两个天体之间的角距离。

## 🎯 项目功能

- **external_test**: 测试调用外部程序（Git、TEMPO等天文工具）
- **user_input**: 交互式计算器，支持用户输入天体坐标
- **astro_math库**: 天文数学函数集合

## 📁 项目结构

```
my_first_software/
├── README.md          # 项目说明
├── Makefile           # 构建配置
├── include/
│   └── astro_math.h   # 函数声明
└── src/
    ├── external_test.c     # 外部程序调用测试
    ├── user_input.c        # 交互式用户输入
    └── astro_math.c        # 数学函数实现
```

## 🚀 快速开始

### 编译
```bash
make
```

### 运行
```bash
./external_test    # 测试外部程序调用
./user_input       # 交互式角距离计算
```

### 安装到系统
```bash
make install       # 安装到 /usr/local/bin
```

安装后可在任何位置运行：
```bash
external_test
user_input
```

### 自定义安装路径
```bash
make install PREFIX=/opt          # 安装到 /opt/bin
make install PREFIX=$HOME         # 安装到 ~/bin
```

### 卸载
```bash
make uninstall
```

## �� 开发

### 清理编译产物
```bash
make clean
```

### 环境要求
- GCC编译器
- Linux/Unix系统
- 数学库 (`libm`)

## 📚 学习内容

这个项目演示了：
1. **C语言模块化编程** - 头文件与源文件分离
2. **Makefile构建系统** - 自动化编译与安装
3. **系统调用** - 通过`system()`调用外部程序
4. **用户交互** - `scanf()`获取输入
5. **Git版本控制** - 标准的开发工作流

## 🌌 天文功能

- 坐标格式转换（时分秒 ↔ 度）
- 球面角距离计算（Haversine公式）
- 天文工具环境检测

## 📄 许可证

学习项目，自由使用。
test
